interface:
  display_name: "SSD Init"
  short_description: "Convierte ideas en specs claras e implementables"
  default_prompt: "Use $ssd-init to turn this feature idea into a compact reviewed spec, then implement only the approved changes without running project scripts or installing dependencies."

policy:
  allow_implicit_invocation: true
